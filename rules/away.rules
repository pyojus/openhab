import org.openhab.core.library.types.*
import org.openhab.model.script.actions.*

rule "Handle Away state"
  when
    Item Away received command OR
  then
    if (receivedCommand == ON) {
      
    }
    
